apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namespace: dev

configMapGenerator:
  - name: app-config
    behavior: create
    envs:
      - app-config.env

secretGenerator:
  - name: app-secret
    envs:
      - .env.app-secret
